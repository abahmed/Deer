language: node_js

os:
  - linux
  - osx
  - windows


install:
  - npm install

script:
  - npm run lint
  - npm run test
  - if [ "$TRAVIS_PULL_REQUEST" == "false" ]; then
      npm run release;
      npm run deploy;
    else
      npm run build-prod;
    fi

branches:
  only:
  - master
